session_name: segment_track

options:
  default-command: /bin/bash

windows:
- window_name: w1
  layout: tiled
  shell_command_before:
    - source ~/segment_slam_ws/src/segment_track_ros/setup.sh
    - $SETUP_SEG_TRACK
    - source $SEG_TRACK_WS/devel/setup.bash

  panes:
  # - roscore
  - roslaunch segment_track_ros husky.launch robot:=hamilton start_rviz:=false seg_viz:=true